  import React from 'react';
  import { Container, Typography } from '@mui/material';

  const Compilations = () => {
    return (
      <Container maxWidth="lg" sx={{ mt: 4 }}>
        <Typography variant="h4">Compilations</Typography>
      </Container>
    );
  };

  export default Compilations;
